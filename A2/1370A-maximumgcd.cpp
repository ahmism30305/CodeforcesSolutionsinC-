#include <bits/stdc++.h>
using namespace std;

int main()
{
    cin.sync_with_stdio(0);
    cout.sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t, x;
    cin >> t;
    while (t--)
    {
        cin >> x;
        cout << x / 2 << '\n';
    }
}